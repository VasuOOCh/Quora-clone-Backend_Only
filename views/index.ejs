<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quora | All Posts</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Quora Posts</h1>

    <% for(let post of posts) { %>
    
        <div class="post">
            <h3>@<%= post.username %></h3>
            <h4><%= post.content %></h4>

            <a class="view" href="http://localhost:8080/posts/<%=post.id %>">View post</a>
            <a class="view" href="http://localhost:8080/posts/<%=post.id %>/edit">Edit</a>
            <form method="post" action="/posts/<%=post.id %>/?_method=DELETE">
                <button style="margin: 10px;">Delete</button>
            </form>
            
        </div>

    <% } %>

    <br>
    <a class="new" href="http://localhost:8080/posts/new">Create New</a>


</body>
</html>